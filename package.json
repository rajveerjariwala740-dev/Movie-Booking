{
  "name": "movie-booking-root",
  "version": "1.0.0",
  "description": "Root package.json for deploying the entire movie booking app",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
